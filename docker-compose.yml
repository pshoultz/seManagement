version: '3'

services:
    api:
        image: img-sem-api
        ports: 
            - 8080:8080
        volumes:
            - db:/app
    monitoring:
        image: img-sem-monitoring
        ports:
            - 8081:8081
        volumes:
            - db:/app

volumes:
    db:
